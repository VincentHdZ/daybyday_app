import 'package:flutter/material.dart';

class ThingsOverviewPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Things Overview Page"),
      ),
    );
  }
}
